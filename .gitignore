.DS_Store
*.log

# Wrangler local state / cache (contains Cloudflare account identifiers).
.wrangler/

# Local source materials — data workbook + original mockup reference.
# Kept in the project folder but never published to GitHub Pages.
source/

# Original camera image; only the optimized assets/about-image.jpg is published.
about-image.heic

# Original full-res show photography; only optimized assets/shows/ is published.
WEBSITE IMAGES/
WEBSITE IMAGES 2/
